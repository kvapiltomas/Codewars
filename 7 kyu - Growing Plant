def growing_plant(upSpeed, downSpeed, desiredHeight):
    height = 0
    step = 1
    while height + upSpeed < desiredHeight:
        height += upSpeed
        height -= downSpeed
        step += 1
    return step
